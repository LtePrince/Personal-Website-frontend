# Public base URL of the site (used for SEO, sitemap, sharing links)
NEXT_PUBLIC_SITE_URL=https://your-domain.example.com

# Backend API base URL (REQUIRED in all environments for server-side fetches)
# Prefer internal/private path from the Next server to the backend in production.
# Examples:
# - Local development (backend on localhost):
#   API_BASE_URL=http://localhost:8080/api
# - Production (same host, backend on 8080):
#   API_BASE_URL=http://127.0.0.1:8080/api
# - Production (different host on LAN):
#   API_BASE_URL=http://10.0.0.12:8080/api
API_BASE_URL=https://your-domain.example.com/api

# Giscus config (public)
# NEXT_PUBLIC_GISCUS_REPO=owner/repo
# NEXT_PUBLIC_GISCUS_REPO_ID=xxx
# NEXT_PUBLIC_GISCUS_CATEGORY=Q&A
# NEXT_PUBLIC_GISCUS_CATEGORY_ID=xxx
